[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
markers =
    network: tests that touch the network (e.g. live FastF1 fetch). Skipped by default; run with `pytest -m network`.
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning:fastf1
