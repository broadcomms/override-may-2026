"""TTM-R2 time-series forecasting (FR-3, optional per graceful-degradation guardrail).

Deferred to v1.1. The v1.0 pipeline accepts ``forecast_fn=None`` and renders
"Forecast unavailable in v1" in the UI per ``docs/04-ui-ux-design.md`` §7.
Model and revision SHA are pinned in ``models.json`` for v1.1 implementation.

See ``docs/06-roadmap.md`` P2.2 and ``docs/adrs/ADR-001-watsonx-runtime.md``.
"""
